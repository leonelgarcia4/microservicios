server:
    port: 8081


spring:
    cloud:
        config:
            server:
                git:
                    uri: https://leonel35197@bitbucket.org/leonel35197/microservicio.git
                    searchPath: config-data
                    username: ${GIT_USERNAME}
                    password: ${GIT_PASSWORD}

    security:
        user: root
        password: secret